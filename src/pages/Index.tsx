import { AppShell } from "@/components/AppShell";
import { Feed } from "@/components/Feed";

const Index = () => {
  return (
    <AppShell>
      <Feed />
    </AppShell>
  );
};

export default Index;
